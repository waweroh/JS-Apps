const array = ["fink", "wing", "jiop", "Diop", "Asern"];
//splice- method  used to  remove and replace new items to an array.
const newArray = array.splice(2, 2, "moses", "thumbs");
console.log(newArray);